<ul>
  <% @tournaments.each do |tournament| %>
    <li><%= link_to tournament.name, tournament_path(tournament) %></li>
  <% end %>
</ul>
<%= button_to "Generate tournament", tournaments_path, method: :post %>
